// export const api_host = window.location.hostname;
// export const api_port = '8080';

export const api_host = '10.10.10.7';
export const api_port = '3005';

export const api_endpoint = `https://${api_host}:${api_port}`;

//'http://'+window.location.hostname+':'